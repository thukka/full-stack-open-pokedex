name: Check service health

on:
  push:
    branches: feat/cronjob

jobs:
    check_health:
        runs-on: ubuntu-20.04
        environment: config
        steps:
            - name: Check URL
              run: echo "test env var ${{ env.PROD_URL }}"
        #   - name: URL Health Check
        #     uses: Jtalk/url-health-check-action@v4
        #     with:
        #       url: ${{ env.PROD_URL }}
        #       run: echo "test env var ${{ env.PROD_URL }}"
        